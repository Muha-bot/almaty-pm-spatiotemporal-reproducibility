# Archived GWR figures

These two PNG files were supplied inside `files (21)(1).zip` and are byte-identical to the previously preserved GWR figure artifacts in the project history. They accompany the 133-station 80/80 GWR dataset and summary already stored under `data/derived/spatial/`.

They are retained as archived empirical outputs, not regenerated from placeholder values.
